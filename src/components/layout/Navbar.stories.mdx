import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
// we have to import this because it is normally imported with layout, but that doesn't happen in this story
import "../../styles/style.scss"
import Navbar from './Navbar';
import { withKnobs, text, boolean, number } from "@storybook/addon-knobs"
import {withSmartKnobs} from "storybook-addon-smart-knobs";



<Meta title="layout/Navbar" component={Navbar} decorators={[withKnobs(), withSmartKnobs()]} />

# Navbar
The site navbar is rendered as part of every page that uses `Layout`.

## Props
<Props of={Navbar} />

## Demo
### Basic

<Preview>
    <Story name="basic">
        <Navbar />
    </Story>
</Preview>

### Admin

<Preview>
    <Story name="admin">
        <Navbar admin />
    </Story>
</Preview>

